# Recomendaciones

- Recordar ejecutar "pod install" en la carpeta ios 
    - puede ser util ejecutar "pod update"
- flutter clean
- flutter pub get
- flutter pub upgrade
- flutter upgrade

ejecutar "flutter clean" y despues "flutter pub get" ayuda a que se actualice la version de la app

# open xcode 
open ios/Runner.xcworkspace
